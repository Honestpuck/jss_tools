<computer_extension_attribute>
  <id>17</id>
  <name>AD Computer Account</name>
  <description>Return the AD computer account</description>
  <data_type>String</data_type>
  <input_type>
    <type>script</type>
    <platform>Mac</platform>
    <script>#!/bin/bash

#

# get ad bound name

#

if [ -n "$(dsconfigad -show)" ]

then

	result=$(dsconfigad -show | grep "Computer Account" | awk '{ print $4 }' | sed "s/\\$//g")

else

	result="NotBound"

fi



echo "<result>${result}</result>"

exit 0

</script>
  </input_type>
  <inventory_display>General</inventory_display>
  <recon_display>Extension Attributes</recon_display>
</computer_extension_attribute>